<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yayiabc.http.mvc.dao.LogisticsDao">

	<select id="queryLog" resultType="com.yayiabc.http.mvc.pojo.jpa.Ordera">
		select orde.shipping_name, orde.shipping_code from ordera  orde join order_item oi on orde.order_id=oi.order_id
		where orde.orde.id=#{orderId} and oi.item_id={itemId}
	</select>
</mapper>